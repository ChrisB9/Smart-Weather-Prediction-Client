import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  	contentBox: {
        flex: 1,
        margin: 5,
        marginTop: 20,
        padding: 5
    }
});

export default styles;
